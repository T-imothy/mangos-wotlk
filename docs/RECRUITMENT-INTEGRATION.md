# Recruitment integration

Register native human invitations with the shared Playerbots coordinator. Process bounded pending work after human session packets and before map workers. Preserve native faction, instance, group capacity and explicit raid conversion policies. Playerbots owns eligibility, deadlines, arrival and preparation checks; native boss scripts and addon files are unchanged.

The shared dependency documents commands, defaults, controlled tests and live-test limits in `docs/RECRUITMENT.md`. This release includes the preceding upstream sync; Wrath still requires its corresponding update 5873/Abduction data and spell 46607 override before using the new binary.
